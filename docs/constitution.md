# Constitution (public summary)

Short free face of *Biology as Code*. Full argument lives in the book.

## Fail-closed

| State | Meaning |
|-------|---------|
| **HOLDS** | Gate open / bound evaluable with declared fields |
| **UNEVALUABLE** | Required field missing — show grey, not fake zero |
| **REFUSE** | Soft verb, tunnel, or category error |
| **OPEN** | Magnitude or primary not locked |
| **REFUTED** | Mechanism walk completed and contradicted the claim — evaluated, and false |

**Empty beats fake.** A refutation is not empty: `REFUTED` is a determinate negative
result, kept distinct from `REFUSE` (declined to evaluate) so the two are never
collapsed.

## Gate ≠ bound

- **Gate** — whether a path can run at all (existence).  
- **Bound** — how far magnitude can move when the path is in scope.  

Do not collapse them. A closed micelle gate is not “slightly less absorption.”

## Four evaluation seats

One law envelope, four questions:

1. **Host** — this machine’s config (stores, stress, ROM)  
2. **Partner** — same-meal co-present fields  
3. **Stage** — where on the L1→L5 path the edge attaches  
4. **Clock** — concurrency / sequence / phase when the packet arrives  

None invents a second constitution.

## Causal stack (L1–L5)

| Tier | Name |
|------|------|
| L1 | Food / matrix |
| L2 | Nutrient cargo |
| L3 | Mechanism (gates live here) |
| L4 | Physiology |
| L5 | Outcome |

Claims that jump L1→L5 without L3 are **malformed**.

## Golden fixtures

- **Non-haem iron** — same label mg, different partners → different bound (ascorbate expands; tea narrows).  
- **Fat vehicle (L-FAT-1 shape)** — no co-present dietary lipid → fat-soluble cargo absorption **absent**, not “reduced.”

See `examples/units/` and `examples/foods/`.

## Product boundary

Optional scores must expand every digit to a law object or return UNEVALUABLE.  
Do not ship forged weights as science.
